part of values;

class Animations {
   
   static const Duration slideAnimationDurationLong = Duration(milliseconds: 1200);
   static const Duration slideAnimationDurationShort = Duration(milliseconds: 1000);
   static const Duration switcherDuration = Duration(milliseconds: 400);
   static const Duration waveDuration = Duration(milliseconds: 1500);
   static const Duration emailSnackBarDuration = Duration(milliseconds: 3500);
   static const Curve textSlideInCurve = Interval(0.6, 1.0, curve: Curves.fastOutSlowIn);
}
